# AtlasBorn client

React + Vite + JavaScript. Run all installation and development commands from the repository root; see [the project README](../README.md).

`src/data/preview.js` contains clearly labeled sample content for Part 1. It must not be reused as authenticated user data. Database credentials belong only in the Express service.
